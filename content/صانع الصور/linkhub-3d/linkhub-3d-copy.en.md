linkhub-3d | صانع الصور (from brief.en.md + tech.md in this pack) | 2026-09-23 | draft-for-editor

## LinkedIn (≤120 words)
LinkHub 3D — a link-in-bio page with real 3D cards, no framework.

Commercial link-in-bio pages are flat, identical lists loading third-party scripts.

I built it in plain HTML, CSS and JavaScript in one file: every card is a physical object with thickness and a highlight; tap it and it performs a true 3D flip, a ring completes in 900 ms, and the whole page recolours to the platform before navigating — every card a real link that works without JavaScript.

Measured in the browser: 8.4 KB gzipped, zero external requests, one screen, every WCAG contrast pair passing.

Is your links page the same template as everyone's? DM me — I'll build one that flips and takes your colour.
github.com/moain2028/linkhub-3d

## X (≤280 chars)
LinkHub 3D: a framework-free links page — cards with a real rotateY flip that recolour the page to the platform, 8.4 KB gzipped, 0 external requests, one screen, every WCAG pair passing, works without JS. github.com/moain2028/linkhub-3d

## Instagram (≤150 words)
Tap the card — it flips, and the page turns Instagram's colour.

LinkHub 3D: a links page for a premium brand in plain HTML, CSS and JavaScript. The card is a physical object: geometric thickness, a highlight that follows your finger, grain. Tap it and it performs a true 3D flip, a ring completes in under a second, and the whole page takes the platform's identity before navigating. A second tap cancels.

The whole page is 8.4 KB gzipped, with not a single external request, on one screen with no scroll. And every card is a real link that works even without JavaScript.

Is your links page the same template as everyone's? DM me — link in bio.

#LinkInBio #CSS3D #WebDesign #RTL #JavaScript

## Reel script (24 s, as rendered)
Cover: LinkHub 3D — real 3D cards with no framework · 8.4 KB gzipped.
1 Problem: A links page like everyone else's — flat identical lists loading third-party scripts, unfit for a premium brand. [No framework · no third-party script]
2 Solution (real browser recording): Tap — it flips and recolours the page — a true 3D flip, a 900 ms ring, and the page taking the platform's identity. [rotateY · preserve-3d · backface-visibility]
3 Material (real browser recording): A physical object, not a coloured rectangle — geometric thickness and a pointer-tracking highlight; every card a real link. [15 material layers on a card face]
By the numbers: 8.4 KB gzipped · 0 external requests · 844=844 one screen · 4.64:1 lowest contrast, all pass WCAG.
CTA: Is your links page the same template as everyone's? DM me and I'll build one that flips and takes your colour. [button: DM me]

<!-- Sources: brief.en.md + tech.md (this pack) ← README + linkhub-audit.json. No person name, no phone number, no social handle. -->

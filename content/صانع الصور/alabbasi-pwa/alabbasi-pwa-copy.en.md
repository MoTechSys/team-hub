alabbasi-pwa | صانع الصور (from brief.en.md + tech.md in this pack) | 2026-09-23 | draft-for-editor

## LinkedIn (≤120 words)
Alabbasi — an offline-first company PWA with zero dependencies.

Company landing pages ship heavy frameworks, break without a connection, and treat Arabic as a second-class language.

I built a company profile in plain HTML, CSS and JavaScript: fully RTL Arabic with Eastern Arabic numerals, a Service Worker with three caching strategies, installable on mobile as a standalone app, and 18 inline SVG icons instead of emoji.

Measured on a real run: 0 dependencies and no build step, ~18.6 KB of text assets gzipped, 7 files precached, and the home page reloads offline straight from the Service Worker.

Does your company site need a strong connection just to show up? DM me and I'll build one that works offline.
github.com/moain2028/alabbasi-pwa

## X (≤280 chars)
Alabbasi: a company page as a PWA — plain HTML/CSS/JS, 0 dependencies, no build, RTL Arabic with Eastern numerals, a Service Worker with three caching strategies, ~18.6 KB gzipped, and it reloads offline. github.com/moain2028/alabbasi-pwa

## Instagram (≤150 words)
Turn off the Wi-Fi — the page is still here.

Alabbasi: a company profile for a digital-services firm, built as a progressive web app in plain HTML, CSS and JavaScript. No framework, no build, no dependencies. Arabic RTL from the root element to the digits to the icons.

A Service Worker precaches 7 files, so the page opens offline exactly as it was, and any uncached route falls back to a clean "offline" page. It installs on mobile as an app with three icons and three shortcuts.

All text assets: ~18.6 KB gzipped — lighter than a single image.

Does your company site need a strong connection just to show up? DM me — link in bio.

#PWA #ServiceWorker #RTL #WebDesign #JavaScript

## Reel script (18 s, as rendered)
Cover: Alabbasi — offline-first PWA, zero dependencies — works offline · fully RTL Arabic.
1 Problem: A company page that dies without Wi-Fi — heavy frameworks, hundreds of dependencies, broken on weak networks, Arabic treated as an afterthought. [No framework · no build · no JS CDN]
2 Solution: A PWA with zero dependencies — plain HTML, CSS and JavaScript, a Service Worker with three caching strategies, installable on mobile as a standalone app. [18 inline SVG icons instead of emoji]
3 Offline: Served straight from the Service Worker — 7 files precached; unknown routes fall back to offline.html — measured on a real run. [Eastern Arabic numerals · full RTL]
By the numbers: 0 dependencies · 18.6 KB gzipped · 7 files precached · 0 console errors.
CTA: Does your company site need a strong connection to show up? DM me and I'll build one that works offline. [button: DM me]

<!-- Sources: brief.en.md + tech.md (this pack) ← README + pwa-audit.json. No person name from the README, no phone number. Page counters (84+ · 3.4× · 97) not used. -->

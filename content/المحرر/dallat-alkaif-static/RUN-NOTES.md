dallat-alkaif-static | المحرر | 2026-09-23 | draft

# RUN-NOTES — التشغيل والقياس والتطميس

## التشغيل
- الثابت: `python3 -m http.server 8080 --bind 127.0.0.1` في جذر المستودع (كما في README) — لا npm ولا build. 20 صفحة/مقاس صُوّرت بلا خطأ كونسول (فقط 404 favicon.ico غير الموجود)، 0 تمرير أفقي على 390×844.
- التوأم Next.js (للمقارنة فقط): `npm install` (280 مجلداً في node_modules) ثم `npx next build` → `out/` بـ 16 HTML؛ يُخدَم تحت `/dallat` (basePath في next.config.ts). خُدم بربط `out/` كمجلد `dallat/` تحت خادم بسيط على 8081.
- المتصفح: chrome@154 عبر puppeteer-core (نفس بيئة keif-aldiafa-system). Google Fonts محجوبة للثابت في القياس كي لا يعتمد الرقم على الشبكة الخارجية.

## القياس (work/dallat/measure3.js) — كاش معطَّل، viewport 390×844 @2x، `networkidle0`
الرئيسية: static 8 طلبات / 558 KB (صور 522 · HTML 36) — next 47 / 1,525 KB (JS 494 · RSC prefetch 524 · صور 341 · HTML 122 · CSS 43).
القهوة: 3 / 70 KB — 30 / 907 KB. الحجز: 4 / 87 KB — 27 / 799 KB.
تحفّظات مذكورة في tech.md: Next تحمّل الصفحات المرتبطة مسبقاً (تنقّل لاحق أسرع)؛ خطوط Next المحلية أعطت 404 على الخادم البسيط بسبب basePath (7–8 طلبات في كل صفحة، بلا بايتات).

## التطميس قبل التصوير (work/dallat/prep.sh — نسخة عمل في /tmp، المستودع لم يُمس)
رقم واتساب/الجوال الموحّد في 13 صفحة (روابط wa.me وtel: والنص المعروض — 40 موضعاً) → `9665XXXXXXXX` / `+966 5X XXX XXXX`. لا أسماء أشخاص في الموقع (شهادات العملاء بلا أسماء). لا JSON-LD في الصفحات.

## إفصاح الصور
كل صور الموقع مولَّدة بالذكاء الاصطناعي بتصريح README («ما زال مطلوباً: صور مناسبات حقيقية — كل الصور الحالية مولَّدة بالذكاء الاصطناعي»). تظهر داخل اللقطات الحقيقية للموقع؛ الإفصاح مكتوب في تذييل كل تصميم يحوي لقطة، وفي SOURCES والشرح.

## اللقطات
desktop-01..10 (2880×1800) · mobile-01..10 (780×1688): home · services · packages · booking · gallery · coffee · coverage · events · sweets · zamzam. الديسكتوب يعرض التخطيط الجوالي في عمود متوسّط (الموقع مبني للجوال بعرض أقصى 480px — README).

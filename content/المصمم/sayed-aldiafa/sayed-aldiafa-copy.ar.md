sayed-aldiafa | المصمم | 2026-09-23 | draft (بانتظار ختم المحرر)

# سيد الضيافة — نصوص النشر

## LinkedIn (≤120 كلمة)
بنيت موقع حفلات ومناسبات بلا إطار عمل. في 2026.

سيد الضيافة: 15 صفحة HTML/CSS/JS خام، عربية RTL بالكامل، جوال-أولاً. صفر اعتمادية وصفر build — الملف الذي تحرّره هو الملف الذي يُخدَم، ويُرفع على أي استضافة كما هو.

الجزء الذي أحبّه: نموذج الحجز لا يحتاج خادماً. 8 حقول تتحوّل بـ JavaScript إلى رسالة واتساب مُعبّأة مسبقاً وتُفتح جاهزة للإرسال.

وتحت السطح: 27 كتلة JSON-LD، Open Graph وcanonical في كل صفحة، 56/56 صورة بنص بديل، لا قفل تكبير.

لمشروع بحجم 15 صفحة هذا قرار صحيح. لو زاد المحتوى، نسخة Next.js موجودة.

المستودع: github.com/moain2028/sayed-aldiafa

## X (≤280 حرف)
موقع حفلات ومناسبات بـ HTML خام: 15 صفحة RTL، صفر اعتمادية، صفر build، ونموذج حجز يتحوّل إلى رسالة واتساب جاهزة بلا خادم. 27 كتلة JSON-LD و56/56 نص بديل.
متى يكون «بلا إطار» هو القرار الصحيح؟
github.com/moain2028/sayed-aldiafa

## Instagram (≤150 كلمة)
موقع يُرفع كما هو ويبيع من الهاتف.

سيد الضيافة — موقع تعهّد حفلات ومناسبات: كوش، جلسات، خيام، بيوت شعر، كراسي، تكييف، صوتيات، قهوجية.

15 صفحة عربية RTL بـ HTML/CSS/JS خام. لا Node، لا build، لا اعتماديات — أي خادم ملفات يكفي.

نموذج الحجز؟ 8 حقول تصير رسالة واتساب جاهزة بنقرة، بلا قاعدة بيانات ولا خادم.

وكل صفحة فيها بيانات مهيكلة ووسوم مشاركة صحيحة، وكل صورة لها نص بديل.

الصور في الموقع مولّدة بالذكاء الاصطناعي حتى تصل صور المناسبات الحقيقية — مذكور صراحة في README.

رابط المستودع في البايو.

## نص الريل (≈28 ث)
[المشكلة] موقع حفلات يُرفع كما هو… ويبيع من الهاتف. بلا Node، بلا build، بلا استضافة خاصة.
[الحل] HTML خام. صفر اعتمادية. الملف الذي تحرّره هو الملف الذي يُخدَم.
[جوال-أولاً] 15 صفحة عربية RTL بالكامل.
[الصفحات] خدمات، باقات، حجز، معرض، تغطية — و9 صفحات خدمة.
[كيف يعمل] النموذج ← JavaScript يبني الرسالة ← wa.me يفتحها جاهزة. بلا خادم.
[الأرقام] 15 صفحة · 0 اعتمادية · 56/56 نص بديل.
[الختام] بلا إطار. بلا build. يعمل في أي مكان. — أبني لمشروعك موقعاً بلا تعقيد.
